project:
    name: messaging-utils-py
    description: Utilities for publishing messages to a messaging system.

layer: library

tasks:
    uv:
        command: uv
        options:
            cache: false

    build:
        command: uv
        args:
            - build

project:
    name: proto-utils-py
    description: |
        Shared utilities for working with protobufs in 
        Python projects.

layer: library

tasks:
    generate:
        command: bash ./scripts/generate.sh
        options:
            cache: false

    build:
        command: uv
        args:
            - build
        deps:
            - ^:generate

project:
    name: eslint
    description: shared eslint config for etl-design packages

tasks:
    pnpm:
        command: pnpm
        args:
            - --filter
            - "@sloth/tools-eslint"
        options:
            runFromWorkspaceRoot: true
            cache: false

    build:
        command: npx
        args:
            - tsup

project:
    name: etl-design
    description: project root

tasks:
    test:
        command: noop
        deps:
            - formula-parser:test
            - ddl-generator:test
            - sql-builder:test
            - excel-reader:test

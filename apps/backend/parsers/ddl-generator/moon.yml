project:
    name: ddl-generator
    description: |
        A Python gRPC service that converts Excel formula Abstract 
        Syntax Trees (AST) into SQL expressions. This service is 
        responsible for translating the hierarchical structure 
        of Excel formulas into equivalent SQL statements that can 
        be used in database queries.

layer: application

language: python

tasks:
    test:
        command: uv
        args:
            - run
            - pytest
            - -v
            - -n
            - auto
            - --cov=src
            - --cov-report=term-missing
            - --cov-report=html
            - --cov-report=xml
        toolchain: python

    uv:
        command: uv
        toolchains: python
        options:
            cache: false

    run:
        command: uv
        args:
            - run
            - python
            - -m
            - src.server
        toolchains: python
        preset: server

    docker-build:
        command: docker
        args:
            - build
            - -f
            - ./apps/backend/parsers/ddl-generator/Dockerfile
            - .
        toolchains: system
        options:
            runFromWorkspaceRoot: true
